Every JSON file in this directoy contains a mapping, encoded as JSON object with:

- the mapping subjects as array of strings in field `subjects`
- the mapping objects as array of strings in field `objects`
- the mapping predicate as string in field `predicate`
- the mapping negativity as boolean in field `negativity`
- the mapping sameness identifier as string in field `id`

